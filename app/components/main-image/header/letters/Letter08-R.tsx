import React from "react";

interface Letter08RProps {
  className?: string;
}

export default function Letter08R({ className }: Letter08RProps) {
  // Color constants
  const lightBrown = "#C88C6A"; // Fill color
  const darkBrown = "#41120A"; // Outline color

  return (
    <g className={className}>
      {/* TODO: Add letter "R" SVG path here */}
      {/* Large, bold, uppercase letter R */}
      {/* Light brown fill with dark brown outline */}
    </g>
  );
}

